{
  "name": "agent-bolek",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply bolek-memory --local",
    "db:migrate:remote": "wrangler d1 migrations apply bolek-memory --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
